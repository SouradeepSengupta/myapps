<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vehicles.list">
      <field name="name">vehicles list</field>
      <field name="model">vehicles.vehicles</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vehicles.action_window">
      <field name="name">vehicles window</field>
      <field name="res_model">vehicles.vehicles</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vehicles.action_server">
      <field name="name">vehicles server</field>
      <field name="model_id" ref="model_vehicles_vehicles"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vehicles" id="vehicles.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vehicles.menu_1" parent="vehicles.menu_root"/>
    <menuitem name="Menu 2" id="vehicles.menu_2" parent="vehicles.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vehicles.menu_1_list" parent="vehicles.menu_1"
              action="vehicles.action_window"/>
    <menuitem name="Server to list" id="vehicles" parent="vehicles.menu_2"
              action="vehicles.action_server"/>
    -->
  </data>
</odoo>